package ch.martinelli.jooqmcp.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for OpenAI embeddings.
 *
 * Spring AI auto-configures the OpenAI embedding model based on properties:
 * - spring.ai.openai.api-key
 * - spring.ai.openai.embedding.options.model
 *
 * This configuration class is a placeholder for any future customizations.
 */
@Configuration
public class OpenAiConfig {
    // Spring AI auto-configuration handles OpenAI embedding client setup
    // via spring-ai-openai-spring-boot-starter
}
